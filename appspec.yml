version: 0.0
os: linux
files:
   -source: /
         destination: /var/www/html
Permissions:
   -object: /var/www/html
         pattern: "*"
         owner: apache
        group: apache
         mode: 755
        type: 
         -file
